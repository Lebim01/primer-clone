import LayoutBody from "@src/layout/Body"
import Layout from "@src/layout/Layout"

const Workflows = () => {
  return (
    <Layout>
      <LayoutBody>
        WORKFLOWS
      </LayoutBody>
    </Layout>
  )
}

export default Workflows